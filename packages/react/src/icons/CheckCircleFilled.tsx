// This icon file is generated automatically.
import * as React from 'react';
import CheckCircleFilledSvg from '@sensoro-design/icons-svg/lib/asn/CheckCircleFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const CheckCircleFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={CheckCircleFilledSvg} />
));

CheckCircleFilled.displayName = 'CheckCircleFilled';

export default CheckCircleFilled;
