// This icon file is generated automatically.
import * as React from 'react';
import TrafficLightFilledSvg from '@sensoro-design/icons-svg/lib/asn/TrafficLightFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const TrafficLightFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={TrafficLightFilledSvg} />
));

TrafficLightFilled.displayName = 'TrafficLightFilled';

export default TrafficLightFilled;
