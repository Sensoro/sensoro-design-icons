// This icon file is generated automatically.
import * as React from 'react';
import GasStationFilledSvg from '@sensoro-design/icons-svg/lib/asn/GasStationFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GasStationFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={GasStationFilledSvg} />
));

GasStationFilled.displayName = 'GasStationFilled';

export default GasStationFilled;
