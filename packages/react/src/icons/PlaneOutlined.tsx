// This icon file is generated automatically.
import * as React from 'react';
import PlaneOutlinedSvg from '@sensoro-design/icons-svg/es/asn/PlaneOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const PlaneOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={PlaneOutlinedSvg} />
));

PlaneOutlined.displayName = 'PlaneOutlined';

export default PlaneOutlined;
