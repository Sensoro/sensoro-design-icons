// This icon file is generated automatically.
import * as React from 'react';
import AimFilledSvg from '@sensoro-design/icons-svg/es/asn/AimFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const AimFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={AimFilledSvg} />
));

AimFilled.displayName = 'AimFilled';

export default AimFilled;
