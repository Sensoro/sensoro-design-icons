// This icon file is generated automatically.
import * as React from 'react';
import NoiseOutlinedSvg from '@sensoro-design/icons-svg/es/asn/NoiseOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const NoiseOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={NoiseOutlinedSvg} />
));

NoiseOutlined.displayName = 'NoiseOutlined';

export default NoiseOutlined;
