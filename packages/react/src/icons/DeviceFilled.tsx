// This icon file is generated automatically.
import * as React from 'react';
import DeviceFilledSvg from '@sensoro-design/icons-svg/lib/asn/DeviceFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const DeviceFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={DeviceFilledSvg} />
));

DeviceFilled.displayName = 'DeviceFilled';

export default DeviceFilled;
