// This icon file is generated automatically.
import * as React from 'react';
import ElectricalFireFilledSvg from '@sensoro-design/icons-svg/lib/asn/ElectricalFireFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ElectricalFireFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ElectricalFireFilledSvg} />
));

ElectricalFireFilled.displayName = 'ElectricalFireFilled';

export default ElectricalFireFilled;
