// This icon file is generated automatically.
import * as React from 'react';
import VolumeCloseOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/VolumeCloseOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const VolumeCloseOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={VolumeCloseOutlinedSvg} />
));

VolumeCloseOutlined.displayName = 'VolumeCloseOutlined';

export default VolumeCloseOutlined;
