// This icon file is generated automatically.
import * as React from 'react';
import HammerFilledSvg from '@sensoro-design/icons-svg/lib/asn/HammerFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const HammerFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={HammerFilledSvg} />
));

HammerFilled.displayName = 'HammerFilled';

export default HammerFilled;
