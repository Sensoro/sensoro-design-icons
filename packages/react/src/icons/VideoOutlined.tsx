// This icon file is generated automatically.
import * as React from 'react';
import VideoOutlinedSvg from '@sensoro-design/icons-svg/es/asn/VideoOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const VideoOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={VideoOutlinedSvg} />
));

VideoOutlined.displayName = 'VideoOutlined';

export default VideoOutlined;
