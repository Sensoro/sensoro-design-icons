// This icon file is generated automatically.
import * as React from 'react';
import GimbalBackDownPurelySvg from '@sensoro-design/icons-svg/es/asn/GimbalBackDownPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GimbalBackDownPurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={GimbalBackDownPurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(GimbalBackDownPurely);

RefIcon.displayName = 'GimbalBackDownPurely';

export default RefIcon;
