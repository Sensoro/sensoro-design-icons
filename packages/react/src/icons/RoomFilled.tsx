// This icon file is generated automatically.
import * as React from 'react';
import RoomFilledSvg from '@sensoro-design/icons-svg/lib/asn/RoomFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const RoomFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={RoomFilledSvg} />
));

RoomFilled.displayName = 'RoomFilled';

export default RoomFilled;
