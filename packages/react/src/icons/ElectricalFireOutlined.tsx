// This icon file is generated automatically.
import * as React from 'react';
import ElectricalFireOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/ElectricalFireOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ElectricalFireOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ElectricalFireOutlinedSvg} />
));

ElectricalFireOutlined.displayName = 'ElectricalFireOutlined';

export default ElectricalFireOutlined;
