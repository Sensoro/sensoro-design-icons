// This icon file is generated automatically.
import * as React from 'react';
import GasAlarmFilledSvg from '@sensoro-design/icons-svg/es/asn/GasAlarmFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GasAlarmFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={GasAlarmFilledSvg} />
));

GasAlarmFilled.displayName = 'GasAlarmFilled';

export default GasAlarmFilled;
