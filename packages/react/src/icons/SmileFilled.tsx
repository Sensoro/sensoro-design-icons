// This icon file is generated automatically.
import * as React from 'react'
import SmileFilledSvg from '@sensoro-design/icons-svg/lib/asn/SmileFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SmileFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={SmileFilledSvg} />
);

SmileFilled.displayName = 'SmileFilled';

export default SmileFilled;
