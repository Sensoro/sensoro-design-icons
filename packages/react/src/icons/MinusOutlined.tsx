// This icon file is generated automatically.
import * as React from 'react';
import MinusOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/MinusOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const MinusOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={MinusOutlinedSvg} />
));

MinusOutlined.displayName = 'MinusOutlined';

export default MinusOutlined;
