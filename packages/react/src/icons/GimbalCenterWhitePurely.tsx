// This icon file is generated automatically.
import * as React from 'react';
import GimbalCenterWhitePurelySvg from '@sensoro-design/icons-svg/es/asn/GimbalCenterWhitePurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GimbalCenterWhitePurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={GimbalCenterWhitePurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(GimbalCenterWhitePurely);

RefIcon.displayName = 'GimbalCenterWhitePurely';

export default RefIcon;
