// This icon file is generated automatically.
import * as React from 'react';
import UsersFilledSvg from '@sensoro-design/icons-svg/lib/asn/UsersFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UsersFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={UsersFilledSvg} />
));

UsersFilled.displayName = 'UsersFilled';

export default UsersFilled;
