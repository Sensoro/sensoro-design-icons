// This icon file is generated automatically.
import * as React from 'react';
import FilterOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/FilterOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FilterOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={FilterOutlinedSvg} />
));

FilterOutlined.displayName = 'FilterOutlined';

export default FilterOutlined;
