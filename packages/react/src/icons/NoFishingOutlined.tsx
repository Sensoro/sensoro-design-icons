// This icon file is generated automatically.
import * as React from 'react';
import NoFishingOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/NoFishingOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const NoFishingOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={NoFishingOutlinedSvg} />
));

NoFishingOutlined.displayName = 'NoFishingOutlined';

export default NoFishingOutlined;
