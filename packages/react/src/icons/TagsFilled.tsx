// This icon file is generated automatically.
import * as React from 'react';
import TagsFilledSvg from '@sensoro-design/icons-svg/es/asn/TagsFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const TagsFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={TagsFilledSvg} />
));

TagsFilled.displayName = 'TagsFilled';

export default TagsFilled;
