// This icon file is generated automatically.
import * as React from 'react';
import WatersOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/WatersOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const WatersOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={WatersOutlinedSvg} />
));

WatersOutlined.displayName = 'WatersOutlined';

export default WatersOutlined;
