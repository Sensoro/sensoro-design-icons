// This icon file is generated automatically.
import * as React from 'react';
import PetDogPurelySvg from '@sensoro-design/icons-svg/lib/asn/PetDogPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const PetDogPurely = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={PetDogPurelySvg} />
));

PetDogPurely.displayName = 'PetDogPurely';

export default PetDogPurely;
