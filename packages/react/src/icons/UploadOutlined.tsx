// This icon file is generated automatically.
import * as React from 'react';
import UploadOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/UploadOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UploadOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={UploadOutlinedSvg} />
));

UploadOutlined.displayName = 'UploadOutlined';

export default UploadOutlined;
