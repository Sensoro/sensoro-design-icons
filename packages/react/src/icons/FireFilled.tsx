// This icon file is generated automatically.
import * as React from 'react';
import FireFilledSvg from '@sensoro-design/icons-svg/lib/asn/FireFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FireFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={FireFilledSvg} />
));

FireFilled.displayName = 'FireFilled';

export default FireFilled;
