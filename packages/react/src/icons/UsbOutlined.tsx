// This icon file is generated automatically.
import * as React from 'react';
import UsbOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/UsbOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UsbOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={UsbOutlinedSvg} />
));

UsbOutlined.displayName = 'UsbOutlined';

export default UsbOutlined;
