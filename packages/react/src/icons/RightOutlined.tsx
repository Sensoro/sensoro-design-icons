// This icon file is generated automatically.
import * as React from 'react';
import RightOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/RightOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const RightOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={RightOutlinedSvg} />
));

RightOutlined.displayName = 'RightOutlined';

export default RightOutlined;
