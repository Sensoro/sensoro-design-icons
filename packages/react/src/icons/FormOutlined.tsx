// This icon file is generated automatically.
import * as React from 'react'
import FormOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/FormOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FormOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={FormOutlinedSvg} />
);

FormOutlined.displayName = 'FormOutlined';

export default FormOutlined;
