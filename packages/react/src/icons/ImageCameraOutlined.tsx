// This icon file is generated automatically.
import * as React from 'react';
import ImageCameraOutlinedSvg from '@sensoro-design/icons-svg/es/asn/ImageCameraOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ImageCameraOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ImageCameraOutlinedSvg} />
));

ImageCameraOutlined.displayName = 'ImageCameraOutlined';

export default ImageCameraOutlined;
