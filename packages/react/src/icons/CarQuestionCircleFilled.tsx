// This icon file is generated automatically.
import * as React from 'react';
import CarQuestionCircleFilledSvg from '@sensoro-design/icons-svg/lib/asn/CarQuestionCircleFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const CarQuestionCircleFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={CarQuestionCircleFilledSvg} />
));

CarQuestionCircleFilled.displayName = 'CarQuestionCircleFilled';

export default CarQuestionCircleFilled;
