// This icon file is generated automatically.
import * as React from 'react';
import GimbalDownWhitePurelySvg from '@sensoro-design/icons-svg/es/asn/GimbalDownWhitePurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GimbalDownWhitePurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={GimbalDownWhitePurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(GimbalDownWhitePurely);

RefIcon.displayName = 'GimbalDownWhitePurely';

export default RefIcon;
