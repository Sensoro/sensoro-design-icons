// This icon file is generated automatically.
import * as React from 'react';
import FileTextProOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/FileTextProOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FileTextProOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={FileTextProOutlinedSvg} />
));

FileTextProOutlined.displayName = 'FileTextProOutlined';

export default FileTextProOutlined;
