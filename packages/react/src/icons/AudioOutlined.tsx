// This icon file is generated automatically.
import * as React from 'react';
import AudioOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/AudioOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const AudioOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={AudioOutlinedSvg} />
));

AudioOutlined.displayName = 'AudioOutlined';

export default AudioOutlined;
