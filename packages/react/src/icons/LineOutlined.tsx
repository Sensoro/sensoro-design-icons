// This icon file is generated automatically.
import * as React from 'react';
import LineOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/LineOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const LineOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={LineOutlinedSvg} />
));

LineOutlined.displayName = 'LineOutlined';

export default LineOutlined;
