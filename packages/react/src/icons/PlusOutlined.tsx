// This icon file is generated automatically.
import * as React from 'react'
import PlusOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/PlusOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const PlusOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={PlusOutlinedSvg} />
);

PlusOutlined.displayName = 'PlusOutlined';

export default PlusOutlined;
