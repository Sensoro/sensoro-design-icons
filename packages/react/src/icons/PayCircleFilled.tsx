// This icon file is generated automatically.
import * as React from 'react';
import PayCircleFilledSvg from '@sensoro-design/icons-svg/lib/asn/PayCircleFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const PayCircleFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={PayCircleFilledSvg} />
));

PayCircleFilled.displayName = 'PayCircleFilled';

export default PayCircleFilled;
