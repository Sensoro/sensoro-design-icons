// This icon file is generated automatically.
import * as React from 'react'
import UserFilledSvg from '@sensoro-design/icons-svg/lib/asn/UserFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UserFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={UserFilledSvg} />
);

UserFilled.displayName = 'UserFilled';

export default UserFilled;
