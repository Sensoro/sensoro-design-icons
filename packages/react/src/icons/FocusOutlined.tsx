// This icon file is generated automatically.
import * as React from 'react';
import FocusOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/FocusOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FocusOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={FocusOutlinedSvg} />
));

FocusOutlined.displayName = 'FocusOutlined';

export default FocusOutlined;
