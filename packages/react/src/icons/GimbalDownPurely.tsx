// This icon file is generated automatically.
import * as React from 'react';
import GimbalDownPurelySvg from '@sensoro-design/icons-svg/es/asn/GimbalDownPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GimbalDownPurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={GimbalDownPurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(GimbalDownPurely);

RefIcon.displayName = 'GimbalDownPurely';

export default RefIcon;
