// This icon file is generated automatically.
import * as React from 'react';
import ImagePurelySvg from '@sensoro-design/icons-svg/es/asn/ImagePurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ImagePurely = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ImagePurelySvg} />
));

ImagePurely.displayName = 'ImagePurely';

export default ImagePurely;
