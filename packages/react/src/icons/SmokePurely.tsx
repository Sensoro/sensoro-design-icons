// This icon file is generated automatically.
import * as React from 'react';
import SmokePurelySvg from '@sensoro-design/icons-svg/lib/asn/SmokePurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SmokePurely = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={SmokePurelySvg} />
));

SmokePurely.displayName = 'SmokePurely';

export default SmokePurely;
