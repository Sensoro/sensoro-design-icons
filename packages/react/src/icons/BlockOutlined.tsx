// This icon file is generated automatically.
import * as React from 'react';
import BlockOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/BlockOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const BlockOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={BlockOutlinedSvg} />
));

BlockOutlined.displayName = 'BlockOutlined';

export default BlockOutlined;
