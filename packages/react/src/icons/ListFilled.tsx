// This icon file is generated automatically.
import * as React from 'react';
import ListFilledSvg from '@sensoro-design/icons-svg/lib/asn/ListFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ListFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ListFilledSvg} />
));

ListFilled.displayName = 'ListFilled';

export default ListFilled;
