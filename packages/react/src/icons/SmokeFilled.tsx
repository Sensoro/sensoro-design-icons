// This icon file is generated automatically.
import * as React from 'react';
import SmokeFilledSvg from '@sensoro-design/icons-svg/lib/asn/SmokeFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SmokeFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={SmokeFilledSvg} />
));

SmokeFilled.displayName = 'SmokeFilled';

export default SmokeFilled;
