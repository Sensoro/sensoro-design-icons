// This icon file is generated automatically.
import * as React from 'react'
import AlertFilledSvg from '@sensoro-design/icons-svg/lib/asn/AlertFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const AlertFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={AlertFilledSvg} />
);

AlertFilled.displayName = 'AlertFilled';

export default AlertFilled;
