// This icon file is generated automatically.
import * as React from 'react';
import SmartCommunityFilledSvg from '@sensoro-design/icons-svg/es/asn/SmartCommunityFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SmartCommunityFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={SmartCommunityFilledSvg} />
));

SmartCommunityFilled.displayName = 'SmartCommunityFilled';

export default SmartCommunityFilled;
