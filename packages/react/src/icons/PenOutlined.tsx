// This icon file is generated automatically.
import * as React from 'react';
import PenOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/PenOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const PenOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={PenOutlinedSvg} />
));

PenOutlined.displayName = 'PenOutlined';

export default PenOutlined;
