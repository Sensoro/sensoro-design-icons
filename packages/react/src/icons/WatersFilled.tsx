// This icon file is generated automatically.
import * as React from 'react';
import WatersFilledSvg from '@sensoro-design/icons-svg/es/asn/WatersFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const WatersFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={WatersFilledSvg} />
));

WatersFilled.displayName = 'WatersFilled';

export default WatersFilled;
