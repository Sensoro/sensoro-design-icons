// This icon file is generated automatically.
import * as React from 'react';
import IllegalFishingFilledSvg from '@sensoro-design/icons-svg/lib/asn/IllegalFishingFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const IllegalFishingFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={IllegalFishingFilledSvg} />
));

IllegalFishingFilled.displayName = 'IllegalFishingFilled';

export default IllegalFishingFilled;
