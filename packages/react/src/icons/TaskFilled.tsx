// This icon file is generated automatically.
import * as React from 'react';
import TaskFilledSvg from '@sensoro-design/icons-svg/es/asn/TaskFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const TaskFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={TaskFilledSvg} />
));

TaskFilled.displayName = 'TaskFilled';

export default TaskFilled;
