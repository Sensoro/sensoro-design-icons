// This icon file is generated automatically.
import * as React from 'react';
import IllegalHelmetPurelySvg from '@sensoro-design/icons-svg/es/asn/IllegalHelmetPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const IllegalHelmetPurely = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={IllegalHelmetPurelySvg} />
));

IllegalHelmetPurely.displayName = 'IllegalHelmetPurely';

export default IllegalHelmetPurely;
