// This icon file is generated automatically.
import * as React from 'react';
import UnitFilledSvg from '@sensoro-design/icons-svg/lib/asn/UnitFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UnitFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={UnitFilledSvg} />
));

UnitFilled.displayName = 'UnitFilled';

export default UnitFilled;
