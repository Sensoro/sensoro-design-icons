// This icon file is generated automatically.
import * as React from 'react';
import TextOutlinedSvg from '@sensoro-design/icons-svg/es/asn/TextOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const TextOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={TextOutlinedSvg} />
));

TextOutlined.displayName = 'TextOutlined';

export default TextOutlined;
