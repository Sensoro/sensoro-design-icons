// This icon file is generated automatically.
import * as React from 'react';
import SafetyOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/SafetyOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SafetyOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={SafetyOutlinedSvg} />
));

SafetyOutlined.displayName = 'SafetyOutlined';

export default SafetyOutlined;
