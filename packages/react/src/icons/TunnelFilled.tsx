// This icon file is generated automatically.
import * as React from 'react';
import TunnelFilledSvg from '@sensoro-design/icons-svg/lib/asn/TunnelFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const TunnelFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={TunnelFilledSvg} />
));

TunnelFilled.displayName = 'TunnelFilled';

export default TunnelFilled;
