// This icon file is generated automatically.
import * as React from 'react';
import VehicleFileFilledSvg from '@sensoro-design/icons-svg/lib/asn/VehicleFileFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const VehicleFileFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={VehicleFileFilledSvg} />
));

VehicleFileFilled.displayName = 'VehicleFileFilled';

export default VehicleFileFilled;
