// This icon file is generated automatically.
import * as React from 'react';
import InfoOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/InfoOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const InfoOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={InfoOutlinedSvg} />
));

InfoOutlined.displayName = 'InfoOutlined';

export default InfoOutlined;
