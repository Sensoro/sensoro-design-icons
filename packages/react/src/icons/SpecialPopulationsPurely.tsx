// This icon file is generated automatically.
import * as React from 'react';
import SpecialPopulationsPurelySvg from '@sensoro-design/icons-svg/es/asn/SpecialPopulationsPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SpecialPopulationsPurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={SpecialPopulationsPurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(SpecialPopulationsPurely);

RefIcon.displayName = 'SpecialPopulationsPurely';

export default RefIcon;
