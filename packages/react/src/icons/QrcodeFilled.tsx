// This icon file is generated automatically.
import * as React from 'react';
import QrcodeFilledSvg from '@sensoro-design/icons-svg/lib/asn/QrcodeFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const QrcodeFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={QrcodeFilledSvg} />
));

QrcodeFilled.displayName = 'QrcodeFilled';

export default QrcodeFilled;
