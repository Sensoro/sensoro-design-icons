// This icon file is generated automatically.
import * as React from 'react';
import DatabaseFilledSvg from '@sensoro-design/icons-svg/lib/asn/DatabaseFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const DatabaseFilled = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={DatabaseFilledSvg} />
));

DatabaseFilled.displayName = 'DatabaseFilled';

export default DatabaseFilled;
