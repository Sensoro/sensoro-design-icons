// This icon file is generated automatically.
import * as React from 'react';
import HumanBodyGalleryPurelySvg from '@sensoro-design/icons-svg/es/asn/HumanBodyGalleryPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const HumanBodyGalleryPurely = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={HumanBodyGalleryPurelySvg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(HumanBodyGalleryPurely);

RefIcon.displayName = 'HumanBodyGalleryPurely';

export default RefIcon;
