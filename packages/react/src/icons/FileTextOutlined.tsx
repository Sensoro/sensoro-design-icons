// This icon file is generated automatically.
import * as React from 'react';
import FileTextOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/FileTextOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const FileTextOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={FileTextOutlinedSvg} />
));

FileTextOutlined.displayName = 'FileTextOutlined';

export default FileTextOutlined;
