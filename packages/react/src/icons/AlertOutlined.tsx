// This icon file is generated automatically.
import * as React from 'react'
import AlertOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/AlertOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const AlertOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={AlertOutlinedSvg} />
);

AlertOutlined.displayName = 'AlertOutlined';

export default AlertOutlined;
