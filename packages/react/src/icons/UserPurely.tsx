// This icon file is generated automatically.
import * as React from 'react';
import UserPurelySvg from '@sensoro-design/icons-svg/lib/asn/UserPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const UserPurely = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={UserPurelySvg} />
));

UserPurely.displayName = 'UserPurely';

export default UserPurely;
