// This icon file is generated automatically.
import * as React from 'react';
import QuestionOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/QuestionOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const QuestionOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={QuestionOutlinedSvg} />
));

QuestionOutlined.displayName = 'QuestionOutlined';

export default QuestionOutlined;
