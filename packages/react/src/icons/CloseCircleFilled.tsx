// This icon file is generated automatically.
import * as React from 'react'
import CloseCircleFilledSvg from '@sensoro-design/icons-svg/lib/asn/CloseCircleFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const CloseCircleFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={CloseCircleFilledSvg} />
);

CloseCircleFilled.displayName = 'CloseCircleFilled';

export default CloseCircleFilled;
