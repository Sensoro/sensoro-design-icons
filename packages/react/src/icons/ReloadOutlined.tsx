// This icon file is generated automatically.
import * as React from 'react';
import ReloadOutlinedSvg from '@sensoro-design/icons-svg/lib/asn/ReloadOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const ReloadOutlined = React.forwardRef<HTMLSpanElement, Partial<SensoroIconProps>>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={ReloadOutlinedSvg} />
));

ReloadOutlined.displayName = 'ReloadOutlined';

export default ReloadOutlined;
