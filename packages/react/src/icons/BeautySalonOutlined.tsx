// This icon file is generated automatically.
import * as React from 'react';
import BeautySalonOutlinedSvg from '@sensoro-design/icons-svg/es/asn/BeautySalonOutlined';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const BeautySalonOutlined = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={BeautySalonOutlinedSvg} />
));

BeautySalonOutlined.displayName = 'BeautySalonOutlined';

export default BeautySalonOutlined;
