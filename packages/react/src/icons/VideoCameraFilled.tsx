// This icon file is generated automatically.
import * as React from 'react'
import VideoCameraFilledSvg from '@sensoro-design/icons-svg/lib/asn/VideoCameraFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const VideoCameraFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>(
  (props, ref) => <SensoroIcon {...props} ref={ref} icon={VideoCameraFilledSvg} />
);

VideoCameraFilled.displayName = 'VideoCameraFilled';

export default VideoCameraFilled;
