// This icon file is generated automatically.
import * as React from 'react';
import SafetySearchFilledSvg from '@sensoro-design/icons-svg/es/asn/SafetySearchFilled';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const SafetySearchFilled = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={SafetySearchFilledSvg} />
));

SafetySearchFilled.displayName = 'SafetySearchFilled';

export default SafetySearchFilled;
