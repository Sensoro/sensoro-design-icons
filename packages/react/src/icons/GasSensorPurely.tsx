// This icon file is generated automatically.
import * as React from 'react';
import GasSensorPurelySvg from '@sensoro-design/icons-svg/es/asn/GasSensorPurely';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const GasSensorPurely = React.forwardRef<HTMLSpanElement, SensoroIconProps>((props, ref) => (
  <SensoroIcon {...props} ref={ref} icon={GasSensorPurelySvg} />
));

GasSensorPurely.displayName = 'GasSensorPurely';

export default GasSensorPurely;
