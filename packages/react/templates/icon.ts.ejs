// This icon file is generated automatically.
import * as React from 'react';
import <%= svgIdentifier %>Svg from '@sensoro-design/icons-svg/es/asn/<%= svgIdentifier %>';
import SensoroIcon, { SensoroIconProps } from '../components/SensoroIcon';

const <%= svgIdentifier %> = (props: SensoroIconProps, ref: React.MutableRefObject<HTMLSpanElement>) => (
  <SensoroIcon {...props} ref={ref} icon={<%= svgIdentifier %>Svg} />
);

const RefIcon: React.ForwardRefExoticComponent<Omit<SensoroIconProps, 'ref'> & React.RefAttributes<HTMLSpanElement>> =
  React.forwardRef<HTMLSpanElement, SensoroIconProps>(<%= svgIdentifier %>);

RefIcon.displayName = '<%= svgIdentifier %>';

export default RefIcon;
